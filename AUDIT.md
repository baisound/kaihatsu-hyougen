# AUDIT.md

## 2026-08-29 構成監査

### 判定

`public/` と `video-top/` を分離した構成は適切です。公開ファイルと動画生成ソースの責務が明確になり、以前よりGit管理しやすくなっています。

### 今回修正した項目

- `public/index.html` のCSS、JavaScript、画像、下層ページ参照を新配置へ変更。
- `public/pages/*.html` のトップ、CSS、JavaScript、画像参照を1階層上へ変更。
- ページ内フラグメントを含むローカルリンクを検査。
- トップの主要ナビゲーションから「支援領域」「つくる人」「相談する」へ実クリックで遷移確認。
- 下部画像を遅延読み込みへ変更。
- 生成動画、収録中間物、QA画像を通常コミットから除外。
- リポジトリの作業境界と更新手順を文書化。

### 良い点

- `public/` が配信ルートとして独立している。
- WebサイトとRemotion動画が別ディレクトリで管理されている。
- CSS、JavaScript、画像が用途別に分かれている。
- 下層ページが `pages/` に集約され、ページ追加位置が明確。
- 画像の正本がサイト側へまとまっている。

### 改善候補

#### 優先度：高

- PNG写真は1枚あたり約2〜3MBあります。表示画像の内容を確定後、品質を確認しながらAVIF/WebP派生を作ると初回表示を改善できます。

#### 優先度：中

- 現在の公開URLは `/pages/services.html` の形式です。動作上は問題ありませんが、公開時のURLを短くするなら `/services/` のディレクトリ形式へ一括移行する方が自然です。
- ヘッダーとフッターが4つのHTMLへ重複しています。ページ数が増える前に静的サイトジェネレーターで共通化すると更新漏れを防げます。

#### 優先度：低

- `video-top/public/company-wordmark.png` はサイト側画像の複製です。動画ビルドの自己完結性を優先するなら現状維持でよいですが、更新時の同期確認が必要です。

### 継続監査項目

- リンク・フラグメント切れ
- PC・スマートフォン表示
- キーボードフォーカス
- `prefers-reduced-motion`
- ブラウザコンソールエラー
- 画像容量と遅延読み込み
- 法人・実名・スタジオ所有を誤認させる文言

### 対応済みの技術項目

- `playwright@1.62.1` を `video-top` の直接devDependencyとして固定。
- HTML画像へ `width` と `height` を追加し、レイアウト移動を抑制。
- 非操作カードから不要な `tabindex="0"` を削除。
- モバイルメニューへスクロール封じ込めとタッチ操作設定を追加。
- 390px幅で下層3ページの大見出しが意図した2行に収まり、横方向のはみ出しがないことを確認。
- トップと下層3ページのHTTP 200、ローカル参照・フラグメント切れ0件、ブラウザ警告・エラー0件を確認。
