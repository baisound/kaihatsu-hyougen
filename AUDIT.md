# AUDIT.md

## 2026-08-29 構成監査

### 判定

`site/`、`_site/`、`video-top/` を分離し、編集元・生成サイト・動画制作の責務が明確になっています。Eleventy導入後もサイトと動画の依存方向は一方向です。

### 今回修正した項目

- `site/index.njk` と `site/<route>/index.njk` のCSS、JavaScript、画像、下層ページ参照を新配置へ変更。
- 下層ページのトップ・共有資産参照を1階層上へ統一。
- ページ内フラグメントを含むローカルリンクを検査。
- トップの主要ナビゲーションから「支援領域」「つくる人」「相談する」へ実クリックで遷移確認。
- 下部画像を遅延読み込みへ変更。
- 生成動画、収録中間物、QA画像を通常コミットから除外。
- リポジトリの作業境界と更新手順を文書化。

### 良い点

- `site/` と `_site/` が編集元と生成物として分離されている。
- WebサイトとRemotion動画が別ディレクトリで管理されている。
- CSS、JavaScript、画像が用途別に分かれている。
- 下層ページがルート別ディレクトリに分かれ、URLと編集位置が一致している。
- 画像の正本がサイト側へまとまっている。

### 改善候補への対応

- 確定済みの写真PNG 6枚からAVIF/WebPを生成し、`picture`でAVIF、WebP、PNGの順に配信する構成へ変更。
- 写真1枚あたり約2.1〜2.4MBのPNGに対し、AVIFは約120〜146KB、WebPは約178〜207KBへ削減。
- 公開URLを `/services/`、`/about-isamu/`、`/contact/` のディレクトリ形式へ一括移行。
- Eleventyを導入し、ヘッダーとフッターを `site/_includes/partials/` へ共通化。
- 動画用ワードマークをサイト正本から同期・検査するスクリプトを追加。
- GitHub PagesでEleventyをビルドし、`_site/` を公開するワークフローへ変更。

### 継続監査項目

- リンク・フラグメント切れ
- PC・スマートフォン表示
- キーボードフォーカス
- `prefers-reduced-motion`
- ブラウザコンソールエラー
- 画像容量と遅延読み込み
- 法人・実名・スタジオ所有を誤認させる文言

### 対応済みの技術項目

- `playwright@1.62.1` を `video-top` の直接devDependencyとして固定。
- HTML画像へ `width` と `height` を追加し、レイアウト移動を抑制。
- 非操作カードから不要な `tabindex="0"` を削除。
- モバイルメニューへスクロール封じ込めとタッチ操作設定を追加。
- 390px幅で下層3ページの大見出しが意図した2行に収まり、横方向のはみ出しがないことを確認。
- トップと下層3ページのHTTP 200、ローカル参照・フラグメント切れ0件、ブラウザ警告・エラー0件を確認。
