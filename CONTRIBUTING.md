# CONTRIBUTING.md

## 基本方針

- 変更はこのGitリポジトリ内で行う。
- 既存の未コミット変更を勝手に破棄しない。
- コミットメッセージとPull Requestは日本語で記載する。
- サイトの表記・色・公開名は `AGENTS.md` に従う。

## ブランチ

必要に応じて `codex/` プレフィックスの作業ブランチを使用する。

例：`codex/fix-public-paths`

## 変更手順

1. `git status --short --branch` を確認する。
2. 対象ファイルと影響範囲を確認する。
3. 相対パスはファイルの配置を基準に記述する。
4. ローカルHTTPサーバーで表示を確認する。
5. PC・スマートフォンの両方で対象ページを確認する。
6. `CHANGELOG.md` の `Unreleased` を更新する。
7. `git diff --check` を実行する。

## レビュー項目

- ローカルファイル・アンカーのリンク切れがない。
- 画像に代替テキストがある。
- キーボードで主要導線へ移動できる。
- フォーカス表示が消えていない。
- `prefers-reduced-motion` が維持されている。
- 法人・実名・スタジオ所有を誤認させる表現がない。
- 生成物や巨大な一時ファイルがステージされていない。

